export const metadata = {
  metadataBase: new URL('https://www.the-pool-lab.com'),
  alternates: {
    canonical: 'https://www.the-pool-lab.com/reviews',
  },
};

export default function ReviewsLayout({ children }) {
  return children;
}
